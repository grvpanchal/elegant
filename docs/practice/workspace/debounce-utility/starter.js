/**
 * debounce(func, wait) -> debounced
 *
 * - calling `debounced` postpones `func` until `wait` ms pass with no new call
 * - a call inside the window resets the timer
 * - `func` receives the LATEST arguments and the `this` of the latest call
 * - `debounced.cancel()` drops a pending call
 */
export default function debounce(func, wait) {
  throw new Error("not implemented");
}
